<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.dd.mapper.StudentMapper">
    <resultMap id="StudentMapper" type="net.dd.pojo.Student">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="usernum" jdbcType="int" property="usernum"/>
        <result column="username" jdbcType="varchar" property="username"/>
        <result column="password" jdbcType="varchar" property="password"/>
        <result column="classname" jdbcType="varchar" property="classname"/>
        <result column="is_deleted" jdbcType="tinyint" property="isDeleted"/>
        <result column="create_time" jdbcType="datetime" property="createTime"/>
        <result column="update_time" jdbcType="datetime" property="updateTime"/>
    </resultMap>

    <insert id="insertStudent">

    </insert>

    <update id="updateStudent">

    </update>

    <delete id="deleteStudent">

    </delete>

    <select id="selectStudent" resultType="net.dd.pojo.Student" resultMap="StudentMapper">
        SELECT * FROM dd_net.student where id = #(id);
    </select>
    <select id="studentLoginCheck" resultType="net.dd.pojo.Student">
        SELECT *
        FROM dd_net.student
        where
        username = #{username}
        and
        password = #{password};
    </select>
    <select id="selectStudentByNumber" resultType="net.dd.pojo.Student">

    </select>
    <select id="selectStudentByName" resultType="net.dd.pojo.Student">

    </select>
    <select id="selectStudentByClassName" resultType="net.dd.pojo.Student">

    </select>

</mapper>