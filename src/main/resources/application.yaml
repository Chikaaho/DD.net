spring:
  mail:
    password: phdkyzggyjjjjggg
    host: smtp.qq.com
    username: chikaho@foxmail.com
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dd_net?serverTimezone=Asia/Shanghai&useSSL=true
    username: root
    password: 123456
  mvc:
    static-path-pattern: /static/**
#  热部署
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8
  devtools:
    restart:
      additional-paths: src/main/java
  freemarker:
    cache: false
mybatis:
  type-aliases-package: net.dd.pojo
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: shutdown